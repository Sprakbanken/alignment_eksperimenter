[project]
name = "alignment-eksperimenter"
version = "0.0.1"
requires-python = ">=3.10"
readme = "README.md"

dependencies = [
    "sentence-transformers>=4.1.0",
    "semchunk>=3.2.1",
    "tqdm>=4.67.1",
    "torch>=2.7.0",
]

[tool.pdm.dev-dependencies]
ruff = []
dev = [
    "ruff>=0.11.11",
    "pre-commit>=4.2.0",
]
